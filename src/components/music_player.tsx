export default ({ src }: { src: string }) => (
    <iframe
        frameBorder="no"
        marginWidth="0"
        marginHeight="0"
        width='100%'
        height={86}
        src={src}
    />
)